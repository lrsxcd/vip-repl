(ns api.leaflet)


(defn create-context [name]
  {:leaflet nil})

(defn leaflet-map-event [leaflet event-id action])

(defn leaflet-map [context  center zoom data_path events]
  {:leaflet})
(defn historgram[context data_path])

(defn piechart [context ])

